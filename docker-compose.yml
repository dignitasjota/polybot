services:
  bot:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PANEL_PASSWORD=${PANEL_PASSWORD:-admin}
      - SESSION_SECRET=${SESSION_SECRET:-}
    volumes:
      - bot-logs:/app/logs
      - bot-data:/app/data

volumes:
  bot-logs:
  bot-data:
