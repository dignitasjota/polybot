# Upper bounds (B11): pin the major so a future breaking release isn't pulled
# in silently. The CLOB V2 client especially must not jump to a V3 that would
# break order signing (see changelog notes).
py-clob-client-v2>=1.0.0,<2.0
py-order-utils>=0.3,<1.0
web3>=6.0,<8.0
websockets>=12.0,<16.0
aiohttp>=3.9,<4.0
structlog>=24.0,<26.0
tomli>=2.0,<3.0;python_version<"3.11"
aiosqlite>=0.19,<1.0
aiohttp-jinja2>=1.6,<2.0
bcrypt>=4.0,<6.0
tomli_w>=1.0,<2.0
py-builder-relayer-client>=0.0.1,<1.0
